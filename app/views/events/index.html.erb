<h1 >Events</h1>
<div>
  <h1>Bienvenue sur Eventbrite</h1>
  <p>Découvrez de nouveaux évènements ou créez les vous-mêmes ! </p>
  <BR>


  <% if !notice.nil? %>
    <div class="alert alert-success" role="alert">
      <p id="notice"><%= notice %></p>
    </div>
  <% end %>
  <table>
    <caption>Voici la liste des évènements du moment...</caption>
    <thead>
      <tr>
        <th>Titre</th>
        <th>Description</th>
        <th>Date de l'évènement</th>
      </tr>
    </thead>
    <tbody>
      <% @events.each do |event| %>
        <tr>
          <td><%= event.title %></td>
          <td><%= event.description %></td>
          <td><%= event.start_date %></td>
          

          <td><%= link_to "Evènement n°#{event.id}", event_path(event.id) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div><BR>
<div>
  <p><a href="<%= new_event_path %>" class="btn btn-primary btn-sm" role="button">Ajoute un évènement</a></p>
</div>

